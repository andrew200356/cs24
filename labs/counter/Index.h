#ifndef INDEX_H
#define INDEX_H

// TODO...
class Index {
   private:
    // Member Variables
    size_t count;       // Number of elements in the index
    size_t capacity;    // Capacity of the index

   public:
    Index(/* args */);
    ~Index();
};

Index::Index(/* args */) {
}

Index::~Index() {
}

#endif
